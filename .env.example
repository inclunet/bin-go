# Use a pooled PostgreSQL connection URL from Neon or Supabase.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Keep this small while Cloud Run is limited to one instance.
DB_MAX_CONNS=5

# Prevent production from silently falling back to in-memory persistence.
REQUIRE_DATABASE=true
